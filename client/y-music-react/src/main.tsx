
import React from 'react';
import ReactDOM from 'react-dom/client';
import PreMain from './Pre-main';


ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <PreMain />
  </React.StrictMode>
);
